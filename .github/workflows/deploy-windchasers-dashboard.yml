name: Deploy Windchasers Dashboard

on:
  push:
    branches:
      - production
      - main
    paths:
      - 'brand/windchasers/dashboard/build/**'
      - '.github/workflows/deploy-windchasers-dashboard.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Increment build version
        working-directory: brand/windchasers/dashboard/build
        run: |
          node scripts/increment-build.js
      
      - name: Commit version bump
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add brand/windchasers/dashboard/build/package.json brand/windchasers/dashboard/build/.build-info
          git commit -m "chore: auto-increment build version [skip ci]" || echo "No changes to commit"
          git push || echo "Nothing to push"
      
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.WINDCHASERS_VPS_SSH_KEY }}
      
      - name: Add VPS to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.WINDCHASERS_VPS_HOST }} >> ~/.ssh/known_hosts
      
      - name: Deploy to VPS via Rsync
        run: |
          rsync -avz --delete \
            --exclude='.env.local' \
            --exclude='node_modules' \
            --exclude='.next' \
            --exclude='.git' \
            --exclude='*.log' \
            --exclude='.DS_Store' \
            -e "ssh -o StrictHostKeyChecking=no" \
            brand/windchasers/dashboard/build/ \
            ${{ secrets.WINDCHASERS_VPS_USER }}@${{ secrets.WINDCHASERS_VPS_HOST }}:/var/www/windchasers-proxe/
      
      - name: Build and Restart on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.WINDCHASERS_VPS_HOST }}
          username: ${{ secrets.WINDCHASERS_VPS_USER }}
          key: ${{ secrets.WINDCHASERS_VPS_SSH_KEY }}
          port: 22
          timeout: 300s
          script: |
            cd /var/www/windchasers-proxe
            
            # Preserve existing .env.local if it exists
            if [ ! -f .env.local ]; then
              echo "⚠️  Warning: .env.local not found on VPS. Please create it manually."
            fi
            
            # Install dependencies (need dev deps for build)
            echo "📦 Installing dependencies..."
            npm ci
            
            # Build the application
            echo "🏗️  Building Next.js application..."
            npm run build
            
            # Check build exit code
            BUILD_EXIT_CODE=$?
            if [ $BUILD_EXIT_CODE -ne 0 ]; then
              echo "❌ ERROR: Build failed with exit code: $BUILD_EXIT_CODE"
              echo "Checking for build errors..."
              exit 1
            fi
            
            # Verify build succeeded
            if [ ! -d ".next" ]; then
              echo "❌ ERROR: Build failed - .next directory not found!"
              echo "Current directory contents:"
              ls -la
              exit 1
            fi
            
            # Check for BUILD_ID (may not exist in all Next.js versions)
            if [ -f ".next/BUILD_ID" ]; then
              echo "✅ BUILD_ID found"
            else
              echo "⚠️  WARNING: BUILD_ID not found, but build may still be successful"
              echo ".next directory contents:"
              ls -la .next | head -10
            fi
            
            echo "✅ Build successful - .next directory exists"
            
            # Restart or start with PM2
            echo "🔄 Restarting application with PM2..."
            PORT=3003 pm2 restart windchasers-proxe || PORT=3003 pm2 start npm --name windchasers-proxe -- start
            
            # Save PM2 configuration
            pm2 save
            
            # Wait for app to start
            sleep 5
            
            # Health check
            curl -f http://localhost:3003/health || echo "⚠️  Health check failed"
            
            # Show PM2 status
            echo "✅ Deployment complete. PM2 status:"
            pm2 list | grep windchasers-proxe || echo "⚠️  Process not found in PM2 list"
